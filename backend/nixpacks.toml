# Railway configuration - installs LaTeX and system dependencies

[phases.setup]
nixPkgs = ["python311", "texlive.combined.scheme-full", "ghostscript"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -w 4 -b 0.0.0.0:$PORT app.api:app"
